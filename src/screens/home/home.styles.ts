import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    paddingTop: 120,
  },
  logo: {
    fontSize: 40,
    color: "#fff",
  },
  buttons: {
    marginTop: 80,
  },
  button: {
    marginBottom: 20,
  },
});
export default styles;
