import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  button: {
    backgroundColor: "#aee81a",
    paddingVertical: 20,
    paddingHorizontal: 35,
    borderRadius: 30,
  },
  button_txt: {
    fontSize: 18,
    color: "#25b0b3",
    textAlign: "center",
  },
});
export default styles;
